## What this changes

## Why

<!--
The diff says what changed. This is for the reasoning that is not recoverable
from it: what you tried, what broke, why the obvious approach does not work.
-->

## How it was verified

- [ ] `ruff check .` and `ruff format --check .`
- [ ] `alembic check` (if models or migrations changed)
- [ ] `pytest`
- [ ] `npm run lint` and `npm run typecheck`
- [ ] `npx jest`
- [ ] Ran against the real app, on: <!-- Android / iOS / web -->

<!--
A bug fix should come with a test that fails without it, not one that merely
exercises the area.
-->
